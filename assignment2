//c program to 10 numbers between 1 and 2 (excluding 2)
#include <stdio.h>
int main()
{
  float n=1.00;
    for(int i=1;i<10;i++)
    {
     n=n+0.10;
     if(n!=2)
     printf("%d %.2f\n",i,n);
     else
     break;
    }
    return 0;
} 
