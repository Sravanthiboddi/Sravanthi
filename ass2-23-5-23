#include <stdio.h>
int main()
{
   int n,sum=0,r,temp=0;
   puts("enter the number");
   scanf("%d",&n);
   temp=n;
   while(n>0)
   {
       r=n%10;
       sum=sum+(r*r*r);
       n=n/10;
   }
  if(temp==sum)
   printf("Armstrong");
  else
   printf("not Armstrong");
    return 0;
}
