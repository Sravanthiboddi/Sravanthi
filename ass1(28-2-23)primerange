write a c program to print count of prime numbers between the given range.
#include <stdio.h>
int main()
{
   int n,t=0,j,f;
   puts("enter the n");
   scanf("%d",&n);
      for(int i=2;i<=n;i++)
        {
            f=0;
            for(int j=1;j<=i;j++)
            {
                if(i%j==0)
                f++;
            }
            if(f==2)  
            {
                
                t = t + 1;
            }
            
        }
        printf("%d",t);
    return 0;
}
